#include <stdio.h>
#include <stdlib.h>
#include "capture14.h"
int main()
{
//    manybook(); // 结构体的声明和结构体数组使用，是一个写书名，作者，价格的代码，遇到输入不成功，达到数量要求或者输入为空时退出
//    friends(); // 一种嵌套结构的使用（以及指针方式的使用，包括两种使用方式）
//    names1(); // 使用指向结构的指针（作为函数传递值的使用方式）
//    names3(); // 使用指针和malloc函数（注意free的使用）
    flexmemb();
}
