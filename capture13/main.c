    /*
     * 第13章：文件输入/输出————
     * 函数：fopen(),getc(),putc(),exit(),fclose(),fprintf(),fscanf(),fgets(),fputc(),rewind(),
     * fseek(),ftell(),fflush(),fgetpos(),feof(),ferror(),ungetc(),setvbuf(),fread(),fwrite()
     * 如何使用C标准I/O函数系列处理文件
     * 文件模式和二进制模式，文件格式和二进制格式，以及缓冲和非缓冲I/O
     * 使用既可以顺序存取文件又可以随机存取文件的函数
     */
#include <stdio.h>
#include <stdlib.h>

int main()
{
    return 0;
}
